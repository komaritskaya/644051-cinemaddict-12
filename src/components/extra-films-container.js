export const createExtraFilmsContainerTemplate = (listTitle) => (
  `<section class="films-list--extra">
    <h2 class="films-list__title">${listTitle}</h2>
    <div class="films-list__container"></div>
  </section>`
);
